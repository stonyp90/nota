{
  "name": "@nota/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Nota admin console (admin.nota.ca) — magic-link auth gate and metrics overview. Vanilla, zero runtime dependencies, strict-CSP safe.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node run-local.mjs"
  }
}
