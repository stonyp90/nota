name: CI

# Fast pre-merge checks (tests + terraform validate, NO deploy). Runs on pull
# requests and on pushes to any branch EXCEPT main — pushes to main are covered
# by deploy.yml, which runs the same test suite before shipping, so ignoring
# main here avoids a redundant duplicate run.
on:
  push:
    branches-ignore: [main]
  pull_request:

# Cancel superseded runs on the same ref to keep the queue fast.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The fast gate (L1 domain · L2 api · L3 contract · L4 dom · L5 BDD). The
  # Playwright browser layer (L6) is deliberately NOT here — it is the only slow
  # layer and runs on the push to main in deploy.yml, before the ship. Keeping
  # it off PRs holds this gate near ~15s so it never discourages a push.
  test:
    name: Tests (domain + api + contract + BDD + web + admin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install workspaces
        run: npm ci

      - name: Unit tests (domain + api)
        run: npm test

      - name: BDD suite (Cucumber)
        run: npm install --prefix features && npm test --prefix features

      - name: Web smoke test (jsdom)
        run: npm test --workspace @nota/web

      - name: Admin console tests (jsdom + dev proxy)
        run: npm test --workspace @nota/admin

  terraform:
    name: Terraform validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Format check
        run: terraform -chdir=infra fmt -check

      - name: Init (no backend)
        run: terraform -chdir=infra init -backend=false

      - name: Validate
        run: terraform -chdir=infra validate
